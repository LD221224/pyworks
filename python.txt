* 변수명
	띄어쓰기 X
	맨 앞에 숫자 X
	예약어 X
	'_'로 연결 ex) var_name

* 자료구조
	리스트 [ ]
	딕셔너리 {key : value}
	튜플 ( ) - 수정, 삭제 불가

* 집합 자료형 set
	중복 불가, 순서 없음
	set() 사용

* 함수
    함수 정의
        def 함수이름:
            실행문
    함수 실행
        함수이름()
    콜백함수 - 재귀함수

* 모듈 - 파이썬 파일(.py)
    변수, 함수, 클래스(객체)가 포함된 소스파일
    외부에서 사용 가능 - import 모듈이름